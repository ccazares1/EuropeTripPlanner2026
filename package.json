{
  "name": "family-trip-planner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "start": "node server/index.js",
    "prestart": "node -e \"console.log('Node version:', process.version); console.log('CWD:', process.cwd()); require('fs').readdirSync('.').forEach(f => console.log(' -', f));\"" 
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^5.1.0"
  }
}
